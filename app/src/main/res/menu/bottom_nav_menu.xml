<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 首页 -->
    <item
        android:id="@+id/nav_home"
        android:icon="@drawable/ic_home_normal"
        android:title="@string/tab_home"/>
    <!-- 短剧 -->
    <item
        android:id="@+id/nav_short"
        android:icon="@drawable/ic_video_normal"
        android:title="@string/tab_video"/>
    <!-- 购物车 -->
    <item
        android:id="@+id/nav_cart"
        android:icon="@drawable/ic_cart_normal"
        android:title="@string/tab_cart"/>
    <!-- 我的 -->
    <item
        android:id="@+id/nav_main"
        android:icon="@drawable/ic_main_normal"
        android:title="@string/tab_main"/>
</menu>